#include <stdio.h>
#include <stdlib.h>

/**
 * main - that is main of course
 *
 * @argc: is counter array in argv
 * @argv: is values in the array
 *
 * Return: Always 0 bro
 */

int main(int argc, char __attribute__((unused)) argv)
{
	printf("%d\n", argc);
	return (0);
}
