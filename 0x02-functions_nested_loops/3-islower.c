#include "main.h"

/**
 * _islower - jdijidjisdjij j ofjkojdcki
 *
 * Return: return 1 if alphabets in lowercase
 * return 0 if otherwise
*/

int _islower(int c)
{
	if (c >= 97 && c <= 122)
		return (1);
	return (0);
}
